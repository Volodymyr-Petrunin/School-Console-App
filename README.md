# School Console App

This sql-jdbc-school application that inserts/updates/deletes data in the database using JDBC.

Also used pattern DAO. Which made it possible to use different jdbc/jpa implementations.
